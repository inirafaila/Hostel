/**
 * workflows/checkout — Checkout Workflow module placeholder.
 *
 * Phase 1: empty stub. Architecture boundary established.
 *
 * Phase 3 scope (from Antigravity Prompt Pack):
 * This workflow must include:
 * - current stay snapshot (guest, bed, dates)
 * - explicit bed post-checkout consequence (→ Dirty)
 * - folio totals review (charges / payments / refunds / balance due)
 * - receivable implication if balance remains
 * - warning / block distinction surface
 * - final sticky confirmation zone
 *
 * Domain note: Checkout of a Stay != cancellation of a Reservation.
 * Bed becomes Dirty after checkout — not Available. Housekeeping must act first.
 */

// export { CheckoutFlow } from './CheckoutFlow'
