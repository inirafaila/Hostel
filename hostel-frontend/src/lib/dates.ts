/**
 * lib/dates.ts — Date formatting and utility helpers.
 * Phase 1: placeholder stubs. Phase 2+: implement using date-fns or similar.
 *
 * No domain business logic here — pure formatting.
 */

/** Format a date to a short operational format: "22 Mar 2026" */
export function formatDate(date: Date | string): string {
  // Phase 2: implement
  return String(date)
}

/** Format a date + time to: "22 Mar 2026, 14:30" */
export function formatDateTime(date: Date | string): string {
  // Phase 2: implement
  return String(date)
}

/** Get a relative label: "Today", "Tomorrow", "Yesterday", or formatted date */
export function formatRelativeDay(date: Date | string): string {
  // Phase 2: implement
  return String(date)
}
