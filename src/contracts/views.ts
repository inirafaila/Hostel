/**
 * contracts/views.ts
 *
 * Phase 1: Placeholder view model contract stubs.
 * View models are composite shapes optimized for screen display —
 * they combine multiple entity shapes into what the UI actually needs.
 *
 * Phase 2+: Implement concrete view model mappers near the
 * entity/feature/workflow that owns the mapping logic.
 * Do NOT create a single giant global mapper file.
 *
 * Examples of composite mappings per Frontend Architecture Blueprint:
 * - ArrivalRowViewModel: reservation + guest + assignment + readiness + payment summary
 * - CheckoutReviewViewModel: stay + bed assignment + folio totals + warnings
 * - MaintenanceBoardRowViewModel: case + affected space + sellability impact
 */

// Placeholder — to be defined in Phase 2 near owning entity/feature modules
// export interface ArrivalRowViewModel { ... }
// export interface CheckoutReviewViewModel { ... }
// export interface FolioTotalsViewModel { ... }
// export interface MaintenanceBoardRowViewModel { ... }

export type _ViewContractPlaceholder = Record<string, never>
