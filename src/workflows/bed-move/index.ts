/**
 * workflows/bed-move — Bed Move Workflow module placeholder.
 *
 * Phase 1: empty stub. Architecture boundary established.
 *
 * Phase 3/4 scope (from Antigravity Prompt Pack):
 * This workflow must include:
 * - current stay snapshot (current bed identity)
 * - candidate bed selection (with eligibility visibility)
 * - target bed eligibility check (gender rule, maintenance, readiness)
 * - reason capture for the move
 * - move consequence summary
 * - final confirmation
 *
 * Must NOT be reduced to a simple dropdown with hidden consequences.
 * Assignment is a temporal occupancy relationship — this is an Assignment change.
 * Bed Move is NOT a Reservation change.
 */

// export { BedMoveFlow } from './BedMoveFlow'
