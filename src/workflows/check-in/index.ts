/**
 * workflows/check-in — Check-In Workflow module placeholder.
 *
 * Phase 1: empty stub. Architecture boundary established.
 *
 * Phase 3 scope (from Antigravity Prompt Pack):
 * This workflow must include:
 * - reservation / guest review step
 * - assignment review step (bed readiness + gender rule check)
 * - warning / block surfacing step
 * - final confirmation step
 *
 * Must NOT be reduced to a single naive button click without
 * review and consequence structure.
 *
 * Workflow orchestration lives HERE, not inside pages/.
 */

// export { CheckInFlow } from './CheckInFlow'
