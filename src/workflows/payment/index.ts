/**
 * workflows/payment — Payment Posting Workflow module placeholder.
 *
 * Phase 1: empty stub. Architecture boundary established.
 *
 * Phase 4/5 scope (from Antigravity Prompt Pack):
 * This workflow must include:
 * - current folio balance context
 * - amount entry
 * - payment method selection
 * - resulting balance visibility
 * - validation summary if invalid
 *
 * Domain note: Payment is posted to a Folio — guest-facing finance.
 * Payment != Expense (internal finance). These must remain conceptually separate.
 */

// export { PaymentFlow } from './PaymentFlow'
